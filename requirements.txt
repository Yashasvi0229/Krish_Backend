# ============================================================================
# GNC Invoice Automation — Backend Dependencies
# Python 3.11+
# ============================================================================

# ---- Web framework ----
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.12          # multipart/form-data (file uploads)
websockets==13.1                  # WebSocket support for job progress

# ---- Pydantic v2 (data validation + settings) ----
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ---- Database: PostgreSQL (async) ----
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0                   # async PostgreSQL driver
alembic==1.13.3                   # migrations
psycopg2-binary==2.9.9            # sync driver (Alembic uses it)

# ---- Redis + Celery ----
redis==5.1.1
celery[redis]==5.4.0
flower==2.0.1                     # Celery monitoring UI (optional)

# ---- Auth / Security ----
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4
cryptography==43.0.1              # Fernet (for encrypting Google refresh tokens)
authlib==1.3.2                    # Google OAuth 2.0
httpx==0.27.2                     # async HTTP client (used by Authlib + integrations)

# ---- Google APIs (Gmail) ----
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.147.0

# ---- Document processing (Phase 2 — Step 4 onwards) ----
openpyxl==3.1.5                   # XLSX read/write (invoice template filler)
pypdf==5.0.1                      # PDF text extraction
python-docx==1.1.2                # DOCX text extraction
pytesseract==0.3.13               # OCR (requires system tesseract binary)
Pillow==10.4.0                    # image handling for OCR
pdf2image==1.17.0                 # convert scanned PDF → image for OCR

# ---- AI providers (Phase 2 — Step 5 onwards) ----
anthropic==0.36.0                 # Claude
openai==1.51.0                    # OpenAI
google-generativeai==0.8.3        # Gemini

# ---- Logging / observability ----
structlog==24.4.0
python-json-logger==2.0.7

# ---- Utilities ----
python-dateutil==2.9.0.post0
tenacity==9.0.0                   # retry decorators (used by AI service)
orjson==3.10.7                    # faster JSON serializer
